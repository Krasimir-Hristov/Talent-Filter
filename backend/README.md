# TalentFilter Backend

FastAPI application for TalentFilter SaaS.

## Tech Stack

- FastAPI
- Pydantic
- Google Gemini API
- Supabase

## Getting Started

1. Install `uv` if you haven't: `pip install uv`
2. Sync dependencies: `uv sync`
3. Run the server: `python -m app.main`
